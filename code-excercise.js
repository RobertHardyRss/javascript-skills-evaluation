// add a method to the person class that returns true if the
// person is an adult (18 or older)

class Person {
	constructor(firstName, lastName, age) {
		this.firstName = firstName;
		this.lastName = lastName;
		this.age = age;
	}

	getFullName() {
		return `${this.firstName} ${this.lastName}`;
	}
}

// create a new person variable

// write a message to the console stating if the person is an
// adult or a child
